# Dependencies
node_modules/

# Build output
# NOTE (MooseLedger fork): dist/ is intentionally committed so the fork is
# installable via `npx -y github:MooseLedgerDev/notebooklm-mcp` without a
# build step. Upstream publishes dist/ via npm; this fork doesn't.
*.tsbuildinfo

# Environment
.env
.env.local

# Data directories (auth & sessions)
data/
browser_state/
chrome_profile/
.notebooklm-mcp/
.claude/
CLAUDE.md

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
screenshots/

# Archives / local notes
*.tar.gz
mcp-add-command.txt
docs/why-notebooklm.md

# Python (from old version)
__pycache__/
*.py[cod]
.venv/
venv/
